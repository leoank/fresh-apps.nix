name: Update Dependencies
on:
  schedule:
    # 06:00 UTC daily — Signal and WhatsApp typically publish during US daytime,
    # so an early-UTC run usually has the prior day's release ready.
    - cron: '0 6 * * *'
  workflow_dispatch:
    inputs:
      packages:
        description: 'Specific packages to update (space-separated, empty for all)'
        required: false
        default: ''
      inputs:
        description: 'Specific flake inputs to update (space-separated, empty for all)'
        required: false
        default: ''
jobs:
  update:
    uses: ./.github/workflows/update-flake.yml
    with:
      packages: ${{ github.event.inputs.packages }}
      inputs: ${{ github.event.inputs.inputs }}
      pr-labels: 'dependencies,automated'
      auto-merge: true
    secrets:
      CLIENT_ID: ${{ secrets.CLIENT_ID }}
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
    permissions:
      contents: write
      pull-requests: write
